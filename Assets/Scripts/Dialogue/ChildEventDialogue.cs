using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChildEventDialogue : MonoBehaviour
{
    //activated through level interaction (picking up, throwing, pushing, etc)
    //pull from random bank of comments

    //some kind of dialogue class (could make new one that better suits needs here)
    //create new method in dialogue manager? then call that method from this script (when activated by player actions?)
    //need TextUI
    //Wont require player interaction to continue or end dialogue
}
